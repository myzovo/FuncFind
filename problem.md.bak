# FuncFind 问题分析与诊断

> 当前日期：2026-05-07 | 状态：已修复 ✅

---

## 1. 核心问题：大模型未真正参与回答生成

### 现象

知识库构建、向量检索链路正常，但用户最终得到的"回答"只是检索结果的机械拼接，而非大模型理解上下文后生成的自然语言回答。

### 根因

两个 `ModelAdapter` 的 `generate()` 方法都是**占位桩（stub）**，没有发送任何 HTTP 请求到 LLM API。

#### WorkersAiModelAdapter.generate()

```java
// 文件: backend/.../service/model/WorkersAiModelAdapter.java:55-82
public String generate(String question, List<RetrievedChunk> contexts, String generationModel) {
    // 只是用 StringBuilder 拼接文本，没有任何 HTTP 调用
    builder.append("[adapter: workers-ai] [model: ").append(model).append("]\n");
    builder.append("问题: ").append(question).append("\n\n");
    // 拼接前 3 条检索结果的前 120 字符预览...
    int limit = Math.min(3, contexts.size());
    for (int i = 0; i < limit; i++) { ... }
    return builder.toString().trim();  // ← 直接返回拼接文本
}
```

#### ExternalModelServiceAdapter.generate()

```java
// 文件: backend/.../service/model/ExternalModelServiceAdapter.java:55-87
public String generate(String question, List<RetrievedChunk> contexts, String generationModel) {
    // 同样只是拼接文本，没有调用 LLM API
    builder.append("[adapter: external] [model: ").append(model).append("]\n");
    builder.append("问题: ").append(question).append("\n\n");
    // ...
    return builder.toString().trim();  // ← 也没有调 LLM
}
```

### 影响范围

| 组件                        | 文件                                                 | 问题                  |
| --------------------------- | ---------------------------------------------------- | --------------------- |
| WorkersAiModelAdapter       | `backend/.../model/WorkersAiModelAdapter.java`       | `generate()` 为占位桩 |
| ExternalModelServiceAdapter | `backend/.../model/ExternalModelServiceAdapter.java` | `generate()` 为占位桩 |
| AnswerGenerationService     | `backend/.../service/AnswerGenerationService.java`   | 纯透传，无增强        |
| ModelRouterService          | `backend/.../service/model/ModelRouterService.java`  | 路由正常，但下游是桩  |

### 当前链路实际行为

```
用户: "如何导出报告？"
    → Embedding 向量化
    → Top-K 检索 (找到 3 条匹配 chunk)
    → WorkersAiModelAdapter.generate()
    → 返回:
      "[adapter: workers-ai] [model: gpt-4o-mini]
       问题: 如何导出报告？

       基于检索上下文生成回答（Workers AI 路由）:
       1. [export-report] 点击 Export Report 按钮...
       2. [download-summary] Download Summary 功能...
       3. [report-page] 报告页面包含..."
```

这本质上是检索结果罗列，不是 LLM 生成。

### 预期行为（修复后）

`ExternalModelServiceAdapter.generate()` 应该：

1. 从 `SessionRuntimeConfig` 获取 `baseUrl` 和 `apiKey`
2. 构建 OpenAI 兼容的 Chat Completions 请求
3. System prompt 中包含检索到的上下文 chunks
4. 发送 HTTP POST 到 LLM API
5. 返回 LLM 生成的自然语言回答

---

## 2. 依赖声明缺失

`crawler/package.json` 只声明了 `crawlee` + `playwright`，但 `server.js` 实际使用了：

| 实际使用的包 | 用途           | 在 package.json 中 |
| ------------ | -------------- | ------------------ |
| `express`    | Web 服务器     | ❌ 缺失            |
| `socket.io`  | WebSocket 通信 | ❌ 缺失            |
| `cors`       | CORS 中间件    | ❌ 缺失            |

**后果**：`npm install` 后运行 `node server.js` 报错退出（exit code 1）。

**修复**：在 `crawler/package.json` 的 `dependencies` 中补充这三个包。

---

## 3. Embedding 也是假的

虽然不影响当前的检索功能（因为存储和查询用的是同一个假实现，一致性保证了检索可用），但两个 Adapter 的 `embed()` 方法也是占位实现：

```java
// WorkersAiModelAdapter.embed()
public List<Float> embed(String text, int dimension) {
    return EmbeddingVectorUtil.deterministicVector(text, dimension);
    // ↑ 是确定性伪向量，不是真正的 embedding 模型生成
}
```

这意味着换成真正的 Cloudflare Workers AI / OpenAI Embedding API 后，需要重建知识库。

---

## 4. 修复优先级建议

| 优先级 | 问题           | 影响             | 修复方向                                           |
| ------ | -------------- | ---------------- | -------------------------------------------------- |
| P0     | LLM 未接入     | 核心功能缺失     | ExternalModelServiceAdapter 真正调 OpenAI 兼容 API |
| P1     | 依赖声明缺失   | crawler 无法启动 | 补全 package.json                                  |
| P2     | Embedding 为假 | 生产不可用       | 接入真实 Cloudflare AI / OpenAI Embedding API      |

---

## 5. 修复涉及的文件清单

```
backend/src/main/java/com/docvecrag/backend/service/model/
├── ExternalModelServiceAdapter.java   ← 核心修复：接入真实 LLM API
├── WorkersAiModelAdapter.java         ← 同上
├── ModelAdapter.java                  ← 可能需要扩展接口
└── ModelRouterService.java            ← 可能需要调整路由逻辑

backend/src/main/java/com/docvecrag/backend/service/
├── AnswerGenerationService.java       ← 可能需要增强 prompt 构建

backend/src/main/resources/
└── application.yml                    ← 已有 external-model-service 配置项

crawler/
└── package.json                       ← 补充缺失依赖
```

---

---

## 6. 修复记录 (2026-05-07)

### P0 - LLM 已接入 ✅
- `ExternalModelServiceAdapter.generate()` 已实现 OpenAI 兼容 Chat Completions API 调用
- `WorkersAiModelAdapter.generate()` 已实现 Cloudflare Workers AI API 调用
- 两个 adapter 都有 fallback 机制，API 不可用时返回检索结果摘要

### P1 - 依赖已补全 ✅
- `crawler/package.json` 已包含 `express`, `socket.io`, `cors`

### P2 - Embedding 已接入 ✅
- `ExternalModelEmbeddingProvider.embed()` 已实现 OpenAI 兼容 Embedding API (`/v1/embeddings`)
- `WorkersAiEmbeddingProvider.embed()` 已实现 Cloudflare Workers AI Embedding API
- 两个 provider 都有 fallback 机制，API 不可用时回退到确定性伪向量

_修复完成。_
