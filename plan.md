严禁的大范围的删除 和重构，除非是非常明显的冗余或低质量代码。主要以增量式的改动为主，保持项目的整体结构和逻辑不变。

【上下文】
这个项目的基本功能已经写完了，能跑起来，但还缺少一些工程上的“成熟感”，目前处于“能用但不够专业”的状态。

【你的任务】
请你以“资深软件工程师”的身份，对项目做一次完全自主的成熟度打磨。你不用逐一询问我，可以自由进行任何你认为必要的修改。你的最终目标是：让这个项目从“能工作”变成“可靠、可维护、可交付”。

【你可以自由做的事情包括（但不限于）】

- 通读整个项目，理解它的目的、结构和现有逻辑
- 直接动手修改代码，添加你认为缺失的部分
- 删除冗余或低质量的代码
- 优化架构、命名、模块划分
- 增强程序的健壮性和可读性
- 任何其他你认为能让项目更专业的改动

【评判“成熟”的参考标准（你修改时请尽量覆盖）】

1. 错误处理与边界情况：所有可能出错的地方是否都有恰当的异常处理？边缘输入是否会崩溃？
2. 配置与参数：硬编码的值是否应提取为配置？配置是否有清晰的默认值和说明？
3. 日志与可观测性：关键路径是否有适度的日志？出错时是否容易定位？
4. 文档与注释：入口文件是否有项目说明？复杂逻辑是否有注释？是否有基本的 API 文档或使用示例？
5. 测试：是否至少有一些基础测试或测试建议？
6. 依赖与环境：依赖是否明确？是否有 requirements.txt / package.json 等？环境安装步骤是否清晰？
7. 安全性：是否存在明显的安全漏洞（如 SQL 注入、明文密钥等）？
8. 代码风格与一致性：是否符合该语言的常见规范？风格是否统一？
9. 可扩展性：新增一个类似功能是否容易？模块之间的耦合是否合理？
10. 资源清理：文件句柄、连接等是否被正确关闭？

【行动方式】

- 第一步，先分析项目现状，给出你发现的“不成熟点”清单。
- 第二步，针对每一个点，逐一进行修改。
- 第三步，修改完成后，总结你改了什么、为什么改、对项目成熟度带来的提升。
- 如果遇到无法确定的业务逻辑问题，可以标注出来并给出建议，但其余工程性优化请直接执行。

请开始。@【上下文】
这个项目的基本功能已经写完了，能跑起来，但还缺少一些工程上的“成熟感”，目前处于“能用但不够专业”的状态。

【你的任务】
请你以“资深软件工程师”的身份，对项目做一次完全自主的成熟度打磨。你不用逐一询问我，可以自由进行任何你认为必要的修改。你的最终目标是：让这个项目从“能工作”变成“可靠、可维护、可交付”。

【你可以自由做的事情包括（但不限于）】

- 通读整个项目，理解它的目的、结构和现有逻辑
- 直接动手修改代码，添加你认为缺失的部分
- 删除冗余或低质量的代码
- 优化架构、命名、模块划分
- 增强程序的健壮性和可读性
- 任何其他你认为能让项目更专业的改动

【评判“成熟”的参考标准（你修改时请尽量覆盖）】

1. 错误处理与边界情况：所有可能出错的地方是否都有恰当的异常处理？边缘输入是否会崩溃？
2. 配置与参数：硬编码的值是否应提取为配置？配置是否有清晰的默认值和说明？
3. 日志与可观测性：关键路径是否有适度的日志？出错时是否容易定位？
4. 文档与注释：入口文件是否有项目说明？复杂逻辑是否有注释？是否有基本的 API 文档或使用示例？
5. 测试：是否至少有一些基础测试或测试建议？
6. 依赖与环境：依赖是否明确？是否有 requirements.txt / package.json 等？环境安装步骤是否清晰？
7. 安全性：是否存在明显的安全漏洞（如 SQL 注入、明文密钥等）？
8. 代码风格与一致性：是否符合该语言的常见规范？风格是否统一？
9. 可扩展性：新增一个类似功能是否容易？模块之间的耦合是否合理？
10. 资源清理：文件句柄、连接等是否被正确关闭？

【行动方式】

- 第一步，先分析项目现状，给出你发现的“不成熟点”清单。
- 第二步，针对每一个点，逐一进行修改。
- 第三步，修改完成后，总结你改了什么、为什么改、对项目成熟度带来的提升。
- 如果遇到无法确定的业务逻辑问题，可以标注出来并给出建议，但其余工程性优化请直接执行。

请开始。
