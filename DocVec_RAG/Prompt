提示词（Prompt）

目标：编写一个适用于网页端轻量部署的 RAG（检索增强生成）系统，重点实现语义精准分块和文档驱动生成，确保在资源有限的网页环境中高效运行。系统需支持用户上传文档后自动处理文本、分块、向量化、存储，并通过用户提问生成精准答案。

核心要求：

网页端轻量部署：

支持浏览器端或轻量后端部署（如 Flask、Node.js 轻量框架）。

优化资源占用：使用轻量级模型和库，减少内存/计算消耗。

支持客户端预处理（如分块）以降低服务器负载。

语义精准分块：

采用混合分块策略：结合固定大小分块（确保效率）与语义分块（保持连贯性）。

使用分层语义建模（如模拟文档结构：章节→段落→句子）或动态合并检索技术。

优先选择轻量但高精度的嵌入模型（如  all-MiniLM-L6-v2 ）进行语义相似度计算。

文档驱动生成：

确保检索到的文本块与问题高度相关，覆盖完整上下文。

大模型生成答案时需明确结合检索到的文档内容，避免信息遗漏。

支持长文档的高效处理，通过分块优化检索精度。

详细步骤与技术要求：

文档上传 → 提取纯文本：

前端支持拖放上传（PDF、DOCX、TXT）。

使用  unstructured  库（或轻量替代方案）提取文本，去除格式信息。

大文件粗切 → 适配小模型：

客户端粗切分：使用前端逻辑（如 JavaScript）按固定大小（如 500 字符）预分块，减少服务器压力。

服务端细切分：结合语义分块策略（如递归分块或基于标题结构）。

轻量AI语义精准分块：

采用两步分块：

初步分块：按段落/章节结构（如标题、子标题）划分。

语义合并：使用  SentenceTransformer  的  all-MiniLM-L6-v2  计算块间余弦相似度，动态合并相邻语义相关块。

设置语义阈值，避免过度合并导致信息稀释。

每块文本生成向量：

使用轻量嵌入模型  all-MiniLM-L6-v2  生成 384 维向量（更轻量且足够精准）。

优化向量计算：批量处理文本块，减少推理延迟。

向量+原文存入向量数据库：

选择轻量级本地向量库（如  FAISS-CPU 、 Chroma ）或云服务（需权衡成本）。

存储时关联文本块元数据（如来源文档、分块ID）。

用户提问 → 问题向量化：

前端输入问题后，使用相同嵌入模型生成问题向量。

优化：缓存常见问题向量，减少重复计算。

向量库相似度检索：

使用余弦相似度检索 top-k 最相关文本块（k=5~10）。

引入动态调整：若结果不足，降低阈值重新检索。

文档+问题喂给大模型 → 生成答案：

构造大模型 prompt 模板，明确区分问题和上下文文档块（按相关性排序）。

使用本地量化模型（如  Llama-3-8B-quantized ）或轻量 API（如  gpt-3.5-turbo ）。

模型提供多样化选择权置于前端选择框


技术栈可听取建议：

后端：Java /springboot

前端：Vue + JavaScript 预处理。

库： unstructured ,  langchain  (分块),  sentence-transformers ,  faiss ,  transformers  (量化模型)。

部署：Docker 容器化

输出要求：

模块化代码：分为前端（上传、交互）、后端（处理、API）、模型服务。

优化配置：提供不同资源环境下的参数选项（如模型切换、分块阈值）。